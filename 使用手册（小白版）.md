# SQL Server → 飞书 同步工具 · 使用手册（小白版）

> 本手册面向零基础用户。跟着步骤一步一步做，就能把 SQL Server 数据库里的订单数据，
> 同步到飞书多维表格，并且自动去重（新增的加进去，已有的更新）。

---

## 一、这个工具能干什么？

把 **SQL Server 数据库表** 里的数据，同步到 **飞书多维表格**。

特点：

- ✅ 全程浏览器操作，有图形界面，不用敲复杂命令
- ✅ 自动去重：按「订单号」判断，新订单新增，老订单更新
- ✅ 支持断点式的进度显示和实时日志

> ⚠️ **重要提示**：本工具只做**从 SQL Server 单向同步到飞书**，不会反过来把飞书数据写回数据库。

---

## 二、需要提前准备什么？

| 需要的东西 | 说明 |
| --- | --- |
| 一台电脑 | Windows 或 macOS 都可以 |
| Python | 3.9 或更高版本（后面教你怎么装） |
| SQL Server 数据库 | 你自己的库，需要知道连接信息 |
| 飞书账号 + 多维表格 | 企业/个人都可以，需要一个多维表格 |

---

## 三、第一步：安装 Python

### Windows
1. 打开网址：https://www.python.org/downloads/
2. 点黄色大按钮 **Download Python 3.x.x** 下载
3. 双击安装包，**一定要勾选**下方的 `Add Python to PATH`（很重要！）
4. 一路点「下一步」直到安装完成

### macOS
1. 打开网址：https://www.python.org/downloads/
2. 同上，点黄色按钮下载 `.pkg` 安装包
3. 双击安装，一路「继续」直到完成

> 装好后终端执行 `python3 --version` 能显示版本号即为成功。（小白可以跳过这步）

---

## 四、第二步：安装本工具的依赖

把整个项目文件夹放到一个**你记得住的位置**（例如桌面或文档目录），然后：

### 方式 A：最简单（推荐小白）
让脚本帮你自动安装依赖。双击下面的文件：

- **Windows**：双击 `install.bat`
- **macOS / Linux**：
  - 在「访达」里右键项目文件夹 → 服务 → 新建位于文件夹位置的终端窗口，然后输入：
    ```
    ./install.sh
    ```
    然后回车。（或者使用方式 B）

脚本会自动：①检查 Python → ②创建独立环境 → ③安装所有依赖。看到 **「安装完成！」** 就是成功了。

### 方式 B：手动安装（懂一点命令行的话）
打开终端，进入项目目录，执行：
```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt     # macOS/Linux
.venv\Scripts\pip install -r requirements.txt # Windows
```

> 📌 说明：本项目把依赖装在项目自己的 `.venv` 文件夹里，**不会破坏你电脑上别的软件**，所以放心装。

### 方式 C：不建独立环境，直接用本机系统 Python
如果你不想建 `.venv`，也可以把依赖直接装到系统 Python 里：

```bash
# macOS / Linux
python3 -m pip install -r requirements.txt

# Windows
pip install -r requirements.txt
```

装完后启动器会自动识别：它**优先找你项目里的 `.venv`，找不到就自动找装了依赖的**系统 Python，你不用手动选择。

> ⚠️ 用系统 Python 的前提：本机 `python3` 指向的那个 Python 装了依赖。如果本机装了多个 Python，启动器会逐个探测、自动挑可用的那个，你无需干预。

---

## 五、第三步：启动程序

双击启动文件：

- **Windows**：双击 `start.bat`
- **macOS**：双击 `start.command`（如果被系统拦截，右键→打开→仍要打开）

看到黑窗口（终端）里出现类似下面的内容，就说明启动成功了：

```
启动面板: http://127.0.0.1:5001
 * Running on all addresses ...
```

**注意**：这个黑窗口**不要关**，关了程序就停了。

然后打开你的浏览器，输入：

```
http://127.0.0.1:5001
```

回车，就能看到工具的配置页面了。

---

## 六、第四步：填写并测试配置

页面上有 4 个步骤，按顺序来。

### 第 1 步：配置 SQL Server

| 字段 | 怎么填 |
| --- | --- |
| SQL Server 地址 | 填数据库地址，如 `localhost` 或 `192.168.1.100`；如果是命名实例填 `localhost\SQLEXPRESS` |
| 端口 | 默认 `1433`，没改过就不用动 |
| 数据库名 | 你的数据库名称 |
| 用户名 | 数据库登录名（如 `sa`）；用 Windows 信任认证可留空 |
| 密码 | 数据库密码（留空表示无密码或 Windows 认证） |
| 数据表名 | 要同步的那张表名，例如 `dbo.Orders` |

填完点 **「测试 SQL Server 连接」**，提示**连接成功**就对了。

### 第 2 步：配置飞书

飞书 App ID / Secret 的获取见下方「七」。多维表格目标**最省事的方式**：直接把「多维表格链接」整条粘贴进去，剩下的 App Token / Table ID / View ID 程序会自动识别，**不用手工分辨**。

| 字段 | 说明 |
| --- | --- |
| 飞书 App ID | 自建应用的 App ID，如 `cli_xxxxxxxxxxx`（获取见 7.1） |
| 飞书 App Secret | 自建应用的密钥（获取见 7.1） |
| 多维表格链接（推荐） | 在飞书打开目标多维表格的**某个数据表**，复制浏览器地址栏整条链接，形如 `https://xxx.feishu.cn/base/bascnXXX?table=tblXXX&view=vewXXX`，粘贴进来即可。**自动识别**，下面三个 App Token/Table ID/View ID 可留空 |
| 多维表格 App Token（可留空） | 备选。多维表格的 App Token，**`bascn`（或 `Bak`）开头**（见 7.3，别和 cli_ 的 App ID 搞混）。填了链接就不用填 |
| 多维表格 Table ID（可留空） | 备选。目标多维表格的 ID，如 `tblxxxxxxxxxx`。填了链接就不用填 |

填完点 **「测试飞书连接」**，提示**连接成功**即可。

### 第 3 步：字段映射（通用，任意表和字段都能用）

这一步把「SQL Server 的列」对应到「飞书表的字段」。工具会自动探测两边的字段，你只需要点几下、选一选。

1. 点 **「① 读取 SQL Server 字段」**——自动列出你的数据表有哪些列
2. 点 **「② 读取飞书表字段」**——自动列出飞书表有哪些列
3. 点 **「自动按同名匹配」**——名字相同（或近似）的列会自动对上；对不上的可以手动从下拉里挑
4. 每一行代表一条对应关系：左边选 SQL 列，右边选它要写进飞书的哪个字段
   - 右边选「不映射 / 留空」表示这列不同步
   - 不需要的对应关系可以点「删除」去掉；少了就点「+ 添加一行」
5. **选「唯一键」**：这是去重的依据，**建议选「订单号」「编号」这类不会重复的字段**。同键时新增，同键已存在则更新。
6. 点 **「保存映射」**，看到「已保存 N 条字段映射」即成功

> 💡 保存后再次打开页面，之前的对应关系会保留。换了数据表或字段，重新读取再保存即可。
> 💡 第 2 步测试飞书连接时如果不通过，第 3 步就读不到飞书字段——请先回去把飞书配好。

### 第 4 步：执行同步

确认没问题后，点 **「开始同步」**。

页面会显示进度条和实时日志，看到 **「同步完成」** 就成功了。可以打开飞书多维表格看看数据是否已经进来。

---

## 七、如何获取飞书 App ID / Secret / Table ID？

这一步最麻烦，小白请耐心看。目的是：创建一个飞书「自建应用」，并给你一个多维表格的权限。

### 7.1 创建自建应用，拿到 App ID 和 Secret
1. 用电脑浏览器打开飞书开放平台：https://open.feishu.cn/
2. 右上角登录你的飞书账号
3. 点左上角 **「创建企业自建应用」**（或「开发者后台」→「创建应用」）
4. 给应用起个名字（随便起，如「订单同步」），点创建
5. 进入应用详情页：
   - **App ID**：在「凭证与基础信息」页面能看到，形如 `cli_xxxxxxxxxxx`
   - **App Secret**：同一个页面，点「查看」按钮后可看到
   - 记住这两串，填到工具第 2 步里

### 7.2 开通多维表格权限
1. 在应用详情页左侧，点 **「权限管理」**
2. 搜索并开通以下权限（找到后点「开通/添加」）：
   - `bitable:app`（查看维空间信息）
   - `bitable:app:readonly`（查看多维表格数据）
   - `bitable:record:create`（新增记录）
   - `bitable:record:update`（更新记录）
3. **重要**：开完权限后，去 **「安全设置」→「发布版本」**，点「创建版本」并发布，权限才会生效！

### 7.3 拿到多维表格的链接（或 App Token 和 Table ID）

**推荐方式：直接复制整条链接**，在工具「飞书配置」第 2 步粘贴「多维表格链接」一项即可，程序会自动识别，无需手工拆字段。

1. 在飞书**多维表格**里创建或打开一张表（例如「订单表」）
2. 看浏览器地址栏 URL，形如：
   ```
   https://xxx.feishu.cn/base/bascnXXXXXXXX?table=tblXXXX&view=vewXXXX
   ```
3. 把这一整条复制，粘到工具里「多维表格链接」即可。

如果你想手动填（备选方式），按下面拆：
   - `/base/` 后面紧跟的那串（**`bascn` 开头**，模拟器或旧版可能是 `Bak` 开头）就是 **App Token**
     - ⚠️ App Token 是 **`bascn`/`Bak` 开头**，**不是**自建应用的 `cli_` App ID，两者别搞混！
   - `?table=` 后面那段（`tbl` 开头）是 **Table ID**
   - `?view=` 后面那段（`vew` 开头，可选）是 **View ID**

### 7.4 关联应用到多维表格
1. 打开你的多维表格，点右上角 **「...」更多 → 设置 → 添加文档应用**（或「关联应用」）
2. 找到你刚创建的应用并添加/关联
3. 这样应用才有权限读写这张表

> 💡 建议飞书字段的名称，和「字段映射」第 3 步里显示的飞书字段名保持一致（如「订单号」「状态」「业务员」等），否则同步时可能找不到对应字段。

---

## 八、常见问题排查

### 0. 双击启动器报错：找不到 Python / 提示未安装依赖
启动器是**自动选 Python** 的：先看项目里的 `.venv`，没有就挨个试本机的 `python3`，谁装了依赖就用谁。如果它提示「没有找到可用的 Python 或依赖」，原因是**机器上所有的 Python 都没装齐依赖**。按顺序处理：

1. **最简单**：双击 `install.bat`（Windows）或 `install.command`（macOS），让它自动建 `.venv` 并装依赖，装完再点启动器。（推荐！环境隔离、不打架）
2. 想用系统 Python 的话，在命令行手动执行：
   ```bash
   pip install -r requirements.txt   # Windows
   pip3 install -r requirements.txt  # macOS/Linux
   ```
   装完再点启动器。
3. **注意**：如果本机装了多个 Python（例如既有 Homebrew 又有系统内置），务必确认执行安装的那个 Python 和启动器用的是同一个。本工具启动器会自动探测，但**安装和探测要针对同一套**。用方式 1（`.venv`）最保险，永远不会搞混。
4. 如果启动了但页面打不开，看启动窗口里的红色报错文字截图，能最快定位。

### 1. SQL Server 连接失败，报错 `DB-Lib error 20009`
数据库连不上。请检查：
- SQL Server 服务是否已启动
- 是否开启了 **TCP/IP 协议**（SQL Server 配置管理器 → 网络配置 → 启用 TCP/IP）
- 端口是否为 1433
- 防火墙是否放行 1433 端口
- 命名实例地址要写 `localhost\SQLEXPRESS` 这种带反斜杠的格式

### 2. 飞书测试失败，或报 **400 Bad Request**（App Token 填错）
- App ID / Secret 是否填对
- 是否**发布过版本**（权限才会生效，见 7.2）
- 是否把应用**关联到目标多维表格**（见 7.4）
- **最常见原因**：把「多维表格 App Token」填成了应用的 App ID（`cli_` 开头）→ 返回 400。**优先用「多维表格链接」粘贴**，让程序自动识别，最不容易错。
- 若解析到链接了但还是报 400，用「测试飞书连接」返回的提示核对；若提示找不到 Table ID，请确认地址栏打开的是具体数据表（URL 含 `?table=tblXXX`）

### 3. 打开 `http://127.0.0.1:5001` 没反应
- 确认启动窗口还在运行、没被关掉
- 确认输入的是 `127.0.0.1` 而不是别的主机名
- 端口可能被占用，可修改 `start.sh` / `start.bat` 里的 `5001` 改成别的（如 `5002`）

### 4. 同步报了权限错误
- 多为权限未开通，或应用未发布、未关联表格。按「七」重新检查一遍。

### 5. 我想重新安装依赖
删掉项目里的 `.venv` 文件夹，再重新运行 `install.sh` / `install.bat`。

### 6. 手动改配置
所有配置保存在项目目录的 `.env` 文件里，想改可以打开编辑，改完重启程序生效。
（密码/Secret 显示为星号是正常现象，是隐藏保护。）

---

## 九、常见疑问

**Q：会不会误删飞书里的数据？**
不会。本工具只做「新增」和「更新」，不会删除任何记录。

**Q：能定时自动同步吗？**
当前版本需要手动点「开始同步」。如果要定时，需要另配定时任务（超出本手册范围）。

**Q：数据很大，同步慢正常吗？**
正常。数据越多越慢，日志会显示进度。飞书接口每次最多批量 500 条。

---

## 十、文件说明

| 文件 | 作用 |
| --- | --- |
| `install.sh` / `install.bat` | 一键安装依赖（第一次用） |
| `start.sh` / `start.bat` / `start.command` | 启动程序 |
| `dashboard.py` | 网页配置面板主程序 |
| `sync.py` | 后台同步数据的核心脚本 |
| `feishu_link.py` | 从多维表格链接自动识别 App Token / Table ID / View ID |
| `mapping.json` | 字段映射配置 |
| `.env` | 数据库和飞书的连接配置（含密码，勿外传） |
| `requirements.txt` | 依赖清单 |
| `README.md` | 原始说明文档 |

---

祝使用顺利！如有问题，对照「八、常见问题」检查，多数都能解决。