# SQL Server → 飞书同步中心：小白使用手册

这套工具可以同时连接两个或更多 SQL Server 数据库。你不需要手写 SQL，也不需要先在飞书逐张建表：在网页里勾选数据库表和字段，系统就能自动创建飞书数据表，并在后续只同步新增或修改的数据。

## 一、先准备什么

### 电脑环境

- Windows、macOS 或 Linux；
- Python 3.10 或更高版本；
- 电脑能够访问 SQL Server 和飞书开放平台。

### SQL Server

为每个数据库准备：

- 服务器地址；
- 端口，通常是 1433；
- 数据库名；
- 用户名和密码。

建议让数据库管理员创建一个专用只读账号，只给需要同步的表或视图 `SELECT` 权限。不要使用 `sa`。

### 飞书

准备：

- 自建应用 App ID；
- App Secret；
- 一个用来存放同步表的飞书多维表格链接。

应用需要记录读取、记录新增、记录更新、数据表新增和字段新增等权限。发布应用版本后，还要把应用添加为目标多维表格的文档应用，并授予可管理权限。

## 二、安装和启动

### Windows

1. 双击 `install.bat`；
2. 安装完成后双击 `start.bat`；
3. 浏览器打开 `http://127.0.0.1:5001`。

### macOS

1. 双击 `install.command`；
2. 双击 `start.command`；
3. 浏览器打开 `http://127.0.0.1:5001`。

如果浏览器没有自动打开，手工输入上面的地址即可。启动窗口不能关闭，关闭后本地服务也会停止。

## 三、配置飞书空间

1. 左侧点击“飞书空间”；
2. 填写 App ID 和 App Secret；
3. 在飞书中打开目标多维表格，复制浏览器地址栏的完整链接；
4. 粘贴到“多维表格完整链接”；
5. 点击“保存并测试”。

测试成功后，页面会列出这个多维表格里已有的数据表。

App Secret 不会回显。以后修改其他配置时，Secret 输入框留空即可保留原值。

## 四、添加两个或更多数据库

左侧点击“数据源”，为每个数据库分别添加一条连接。

例如：

- 数据源一：订单库，`192.168.1.10 / OrdersDB`；
- 数据源二：库存库，`192.168.1.20 / StockDB`。

每条连接填写：显示名称、服务器、端口、数据库、只读用户名和密码。保存后点击“测试连接”。

如果你只知道服务器，不知道数据库名：

1. 数据库先留空保存，系统会先连接 `master`；
2. 点击“识别可访问数据库”；
3. 从建议列表中选择数据库；
4. 再保存并测试。

同一服务器上的两个数据库，也可以保存成两个独立数据源，后面分别勾选表。

## 五、勾选需要同步的表

1. 左侧进入“同步设计器”；
2. 选择一个数据源；
3. 点击“读取数据表”；
4. 页面会显示这个数据库中可访问的数据表和视图；
5. 勾选需要同步的表，进入字段配置。

每勾选一张表，就会建立一个独立同步任务。不同数据库的表互不影响。

## 六、勾选需要同步的字段

进入某张表的配置后：

1. 勾选要同步到飞书的字段；
2. 右侧默认使用 SQL 字段名作为飞书字段名，也可以修改；
3. 选择唯一键；
4. 决定是否启用增量同步；
5. 保存任务。

### 唯一键怎么选

唯一键用来判断一条数据是新增还是更新。建议选择：订单号、明细 ID、业务编号等不会重复且不能为空的字段。

如果 SQL 数据中唯一键重复或为空，程序会停止该任务，不会冒险写入。

### 增量字段怎么选

推荐选择：

- `UpdatedAt`、`ModifiedTime` 等更新时间；
- 或者持续递增的 ID。

不要选择经常变小、会被重置、或者大量为空的字段。

## 七、自动建飞书表还是绑定已有表

### 自动匹配或新建表

这是推荐方式。你只需要填写飞书表名：

- 飞书中已有同名表，系统使用它；
- 没有同名表，正式同步时自动创建；
- 缺少字段时，勾选“自动补建飞书字段”即可自动创建。

预检不会真的建表，只会告诉你计划。

### 选择已有表

如果希望写入某张已有飞书表：

1. 先到“飞书空间”测试连接，让系统读取已有表；
2. 在任务里把飞书目标改为“选择已有表”；
3. 选择目标表。

这种方式属于明确绑定。其他任务仍然可以使用自动建表，不是全局强制绑定。

## 八、先预检，再同步

进入“执行与日志”：

1. 点击“预检全部任务”；
2. 查看每个任务的读取、新增、更新和跳过数量；
3. 数量符合预期后，点击“同步全部任务”；
4. 运行中可以停止，也可以刷新页面后继续查看状态。

第一次运行通常是全量。成功后，启用增量的任务会保存游标；以后只读取游标之后的数据。

## 九、增量同步为什么不会轻易漏数据

程序使用“包含上次游标”的方式重新读取边界数据。例如上次最大更新时间是 10:00，下次从“更新时间大于等于 10:00”开始。10:00 的旧数据会重新比较，但内容没变化就跳过；这样能避免同一时间点新增多条记录时遗漏。

游标只有在整张表同步成功后才更新。任务失败或停止不会推进游标。

改了配置也不用担心漏数据：只要你改动了这个任务的**数据源、勾选字段、唯一键、增量字段或目标飞书表**中的任何一项，旧游标就会自动作废，下次运行自动改为全量重新扫描，日志里会写“已重置增量游标”。数据库把增量字段类型改掉了（比如从时间改成编号），程序也会自动回退全量。

如果想主动重新全量检查，在任务卡片点击“重置游标”。

## 十、哪些字段可以自动处理

- SQL 整数、小数、金额、浮点 → 飞书数字；
- SQL 日期和时间 → 飞书日期；
- SQL bit → 飞书复选框；
- 其他常见字段 → 飞书文本。

人员、附件、公式、关联记录等复杂字段需要特殊格式，当前不建议直接映射普通 SQL 文本。如果绑定已有表并遇到不支持的复杂字段，预检会停止并提示。

## 十一、常见问题

### SQL 连接失败

检查服务器地址、端口、TCP/IP、防火墙、数据库名和账号权限。命名实例可能需要数据库管理员确认实际端口。

### 识别不到数据库

账号可能没有查看其他数据库的权限。可以直接填写已知数据库名，或者让数据库管理员授权查看和读取目标数据库。

### 飞书能连接，但不能建表或字段

应用可能只有记录读写权限，没有数据表或字段管理权限；也可能没有被授予目标多维表格的可管理权限。

### 预检提示重复唯一键

先在 SQL 或飞书中处理重复数据。不要通过修改程序绕过检查，否则增量更新无法判断应该更新哪一行。

### 我不想增量，只想每次检查全部数据

关闭任务的“启用增量同步”即可。程序每次读取全表，但仍只写入新增或发生变化的记录。

### SQL 里删除的数据会同步删除吗

不会。工具只新增和更新，不自动删除飞书记录，避免误删。如果以后需要删除同步，应单独设计软删除字段和确认流程。

## 十二、重要文件

- `.env`：飞书 Secret 和各数据源密码；
- `sync_config.json`：数据库、表、字段和任务配置，不含密码；
- `sync_state.json`：增量游标；
- `sync.log`：同步日志；
- `dashboard_server.log`：后台错误日志。

不要把 `.env` 发给别人或提交到公开仓库。第一次正式同步前，建议先复制一份飞书目标表作为备份。
