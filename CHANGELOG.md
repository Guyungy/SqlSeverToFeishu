# 更新日志 (Changelog)

> 版本按功能/修复里程碑记录。分发包命名沿用 `SqlSeverToFeishu_分发版_YYYY-MM-DD.zip`。

## [2.1.0] - 2026-09-16

### 新增
- **运行设置搬进网页**：查询超时、SQL NULL 处理、时区偏移、服务端口现在都在界面「运行设置」里改，保存到 `sync_config.json` 的 `settings` 段，改完即生效，不再需要手工编辑 `.env`。

### 变更
- 配置来源统一为 **界面设置 > `.env` > 程序默认值**，页面会标出每一项当前取自哪里。服务端口按启动参数语义处理：命令行显式 `DASHBOARD_PORT` 时以命令行为准。
- `.env` 变为可选项，由页面自动维护；原 `SYNC_NULL_POLICY=clear` 继续等同于 `overwrite`，不会被静默降级为 `skip`。
- 时区偏移与 NULL 处理纳入增量游标指纹：改动这两项会自动作废游标并全量重扫；查询超时不影响值语义，改它不会触发重扫。
- 配置文件版本升至 3（旧版本 2 配置无 `settings` 段时仍可正常读取，自动补默认值）。
- `start.sh` / `start.bat` 不再硬编码 `DASHBOARD_PORT=5001`（此前会覆盖界面里配置的端口），改由 `dashboard.py` 统一解析并打印实际地址。

### 修复
- `start.bat` 的 Python 版本判断同样含多余的 `^` 转义，会导致 Windows 上跳过所有 Python 环境并报“找不到可用环境”；同时版本要求由 3.9 更正为 3.10。
- `start.sh` 的 Python 版本判断仍为 3.9，与依赖要求不一致，已统一为 3.10。

## [2.0.1] - 2026-09-16

### 修复
- **增量游标与任务语义绑定**：此前 `job_fingerprint` 已定义但从未调用，改动增量字段后会把旧游标套用到新字段上，导致任务抛类型错误；若两个字段类型相近则会静默漏数据。现在游标与「数据源 + 字段 + 唯一键 + 增量字段 + 目标飞书表」绑定，任一项变化即自动作废并全量重扫，日志与结果中明确提示“已重置增量游标”。
- 游标类型与 SQL 列类型不匹配时（例如数据库把 `datetime` 改成 `int`）自动回退全量扫描，不再带着错类型查询。
- 修复自动补建飞书字段时的 `create_field()` 参数缺失：在已有飞书表上补建缺字段必定抛 `TypeError`。
- 目标飞书表的字段类型预检改为真正生效：唯一键落在只读或复杂字段（附件、人员、公式等）上时，预检阶段即报错，不再等到写入才失败。
- 唯一键与唯一键规范化改用实际目标字段类型判定，避免与建表时声明的类型不一致。
- 修复同步设计器“串库”：编辑任务时数据源改用编辑器内锁定的数据源，中途切换数据源会关闭编辑面板，避免任务被保存到另一个数据库上。
- `install.bat` 的版本判断包含多余的 `^` 转义，导致 Windows 上一律报“Python 版本不足”；同时 README、使用手册与安装脚本的版本要求由 3.9 更正为 3.10（`python-dotenv`、`requests` 实际要求 ≥3.10）。

### 其他
- 仅在 Table ID 真正变化时才回写配置，缩短并发写入窗口。
- `.gitignore` 补充忽略 `.workbuddy-ai/`。
- 回归测试由 19 项扩充到 27 项，新增游标失效、类型不匹配回退、目标字段类型校验和补建字段用例。

## [2.0] - 2026-09-12

### 多数据源与交互式配置
- 支持添加多个 SQL Server 数据源，可连接不同服务器或不同数据库。
- 新增数据库、表、视图和字段自动探测。
- 新增勾选式同步设计器：按表选择、按字段选择、设置唯一键和目标字段名。
- 每张 SQL 表独立成为同步任务，可单独启用、编辑、预检或删除。

### 飞书自动建档
- 支持在指定多维表格中按名称匹配或自动创建数据表。
- 根据 SQL 数据类型自动创建文本、数字、日期和复选框字段。
- 支持绑定已有飞书表，也支持为已有表自动补建缺失字段。

### 增量同步
- 首次执行全量读取，成功后为每个任务保存独立增量游标。
- 后续按更新时间或递增 ID 读取增量数据，包含游标边界以避免同时间戳数据遗漏。
- 仅在整张表写入成功后推进游标；支持一键重置为全量。
- 新增 `sync_config.json`、`sync_state.json`、`workspace.py` 和 `multi_sync.py`。

## [1.3] - 2026-09-12

### 安全与一致性
- 修复查重失败时误判为新记录的问题；改为分页读取目标记录并在失败时停止。
- 修复管理后台保存普通配置时可能用星号覆盖密码或 App Secret 的问题。
- 严格校验唯一键、源端重复键、目标端重复键和字段映射。
- 限制 SQL 表名格式，并通过元数据白名单验证列名。
- 配置和字段映射改为原子写入，`.env` 强制使用仅当前用户可读权限。

### 性能与稳定性
- 飞书请求增加超时、指数退避重试、限流处理和 Token 刷新。
- SQL 数据分批读取，飞书记录一次分页加载，移除逐行查重请求。
- 跳过内容未变化的记录；批量创建请求在重试周期内复用 UUIDv4 幂等标识。
- 增加跨进程同步锁、后台任务管理、停止按钮和日志轮转。

### 使用体验
- 新增“仅预检”模式，正式写入前展示新增、更新和跳过数量。
- 配置改为显式保存，密钥不回显且留空保持原值。
- 管理后台增加状态门禁、任务恢复、移动端布局和无障碍标签。
- 增加核心回归测试，固定经过验证的依赖版本。

## [1.2] - 2026-09-09

### 新增
- **支持粘贴飞书多维表格完整链接**，自动识别 `app_token` / `table_id` / `view_id`，不再需要手工分辨 `bascn`/`Bak`/`tbl`/`vew`。
  - 新增 `feishu_link.py`：解析 `/base/{app_token}?table=&view=`、`?tbl=`、裸 token 等形态。
- 网页配置面板新增「多维表格链接」输入项（推荐用法），并补字段填写提示。

### 修复
- 附带上一里程碑的 400 修复（详见 [1.1.1]）。

### 变更
- 配置字段 `FEISHU_BASE_APP_TOKEN` / `FEISHU_BASE_TABLE_ID` / `FEISHU_BASE_VIEW_ID` 由「必填」改为「可选」：填了链接即无需再填。
- 文档（README / 使用手册 / .env.example）统一为「粘贴整条链接」优先的说明。

## [1.1.1] - 2026-09-08

### 修复
- **修复飞书 400 Bad Request**：旧逻辑误把应用 App ID（`cli_` 开头）当作多维表格 app_token 调用 `/bitable/v1/apps/{...}/tables`。
  - 新增独立配置 `FEISHU_BASE_APP_TOKEN`，`dashboard.py` / `sync.py` 统一从它取值；App ID/Secret 仅用于换取 `tenant_access_token`。

## [1.1] - 2026-09-08

### 新增
- 通用化改造：自动探测 SQL Server 表字段与飞书表字段，可视化下拉映射、按同名自动匹配、可增删映射行。
- 可选唯一键：按业务主键判断「新增 / 更新」，实现自动去重。

## [1.0] - 2026-09-02

### 新增
- 首个可用版本：SQL Server → 飞书多维表格单向同步工具。
- 网页配置面板（Flask）+ 后台同步脚本（pymssql）。
- 一键安装脚本（`install.sh` / `install.bat`）与零基础《使用手册》。