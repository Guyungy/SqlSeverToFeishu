#!/usr/bin/env bash
# ============================================================
# macOS 双击安装文件
# 双击本文件即可创建独立环境 .venv 并安装全部依赖
# ============================================================
cd "$(dirname "$0")"
./install.sh